version: 2
jobs:
  build:
    docker:
      - image: docker:17.10.0-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run: echo "A first hello"
      - run:
          name: docker run
          command: |
            docker run -it --rm -v $PWD:/home/neuro/nipype_tutorial djarecka/nipype_tutorial pytest /home/neuro/nipype_tutorial/test_notebooks.py
         